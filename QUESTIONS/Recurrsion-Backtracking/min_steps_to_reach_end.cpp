#include<bits/stdc++.h>
#include <iostream>
using namespace std ;


int steps (vector<int> & nums, int index) {
    if (index == 0) {
        return 0 ;
    }
    return min()
}

int main()
{
    int n ;
    cin >> n ;
    vector<int> nums(n) ;
    for (int i = 0; i < n; i++)
    {
        cin >> nums[i] ;
    }
    cout << steps(nums, nums.size()) ;
    return 0 ;
}