#include<bits/stdc++.h>
#include <iostream>
using namespace std ;

int main()
{
    int t ; 
    cin >> t;
    while (t--) {
        pair<int,int> input , ans, temp = {0,0} ;
        cin >> input.first >> input.second ;
        temp.first +=  input.first ;
        temp.second += input.second ;

        

    }
    return 0 ;
}