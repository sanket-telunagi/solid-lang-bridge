#include<bits/stdc++.h>
#include <iostream>
using namespace std ;
bool hasAlternatingBits(int n) {
    int curr, next = n & 1;
    while (n)
    {
        
    }
    return true ;
}
int main()
{
    int num ;
    cin >> num ;
    cout << hasAlternatingBits(num) ;
    return 0 ;
}