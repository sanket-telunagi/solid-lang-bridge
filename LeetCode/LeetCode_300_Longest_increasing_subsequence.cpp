#include<bits/stdc++.h>
#include <iostream>
using namespace std ;

int fun(int idx, vector<int> & nums) {
    
}

int lengthOfLIS(vector<int>& nums) {
    
}

int main()
{
    int n ;
    cin >> n ;
    vector<int> nums(n) ;
    for (int i = 0; i < n; i++)
    {
        cin >> nums[i] ;
    }
    cout << lengthOfLIS(nums) ;
    return 0 ;
}